# PR to PO Master notebook — dependencies
# Production (AWS): add boto3, langchain-aws
# Local: Groq + sentence-transformers (optional for embeddings)

# Core
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.2.0

# LLM — local (Groq)
langchain-groq>=0.1.0

# LLM — production (uncomment for AWS)
# langchain-aws>=0.1.0
# boto3

# Document loading
pypdf>=4.0.0

# DB
sqlalchemy>=2.0.0

# Schema validation (for parsing agentic checks)
jsonschema>=4.0.0

# Env
python-dotenv>=1.0.0

# Optional: Word docs
# python-docx>=1.0.0

# Optional: Excel (for PR form / header+line items extraction and classification)
openpyxl>=3.0.0

# Optional: local embeddings (for RAG later)
# sentence-transformers>=2.2.0
