# PR to PO Master notebook — dependencies
# Aligned with company-approved package set (S2P Backend / RAG). Groq is our own.
# Python 3.10.x. Use a dedicated conda env (e.g. pr2po), not base, to avoid conflicts.

# --- Company-approved versions (from S2P Backend / RAG list) ---

# Data validation
pydantic>=2.9.2
pydantic-settings>=2.1.0

# Environment variables
python-dotenv==1.2.1

# HTTP client
httpx>=0.25.0

# LangChain (approved)
langchain==1.1.2
langchain-text-splitters
langchain-community==0.4.1

# LLM — local (our own; not in company list)
langchain-groq>=0.1.0

# LLM — production (AWS)
boto3==1.42.4
langchain-aws==1.1.0
botocore>=1.34.0

# Document processing
pypdf==6.4.0
python-docx==1.2.0
openpyxl==3.1.5

# Database (sync; notebook uses SQLite/PostgreSQL via psycopg2)
sqlalchemy>=2.0.25
# Schema validation (parsing / compliance checks)
jsonschema>=4.0.0

psycopg2-binary==2.9.11

# JSON & numeric
orjson>=3.9.0
numpy>=1.24.0

# --- Not in approved list (needed for notebook; remove or get approval if required) ---
# Schema validation (parsing / compliance checks)
jsonschema>=4.0.0

# Optional: LangGraph (extract graph in master notebook). Omit if not approved.
langgraph>=0.2.0

# RAG / vector (Section 7–8). Required per mode.
# AWS mode: opensearch-py for OpenSearch; Titan via langchain-aws (already above).
# Local mode: qdrant-client for Qdrant Cloud; sentence-transformers for embeddings.
qdrant-client>=1.7.0
opensearch-py>=2.4.0
sentence-transformers>=2.2.0
